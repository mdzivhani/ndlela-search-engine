# API Contract Rules

- All APIs must follow REST or defined protocol rules.
- Each endpoint must include:
  - Endpoint route
  - Request format
  - Response format
  - Error patterns
  - Auth requirements

Store example request and response bodies in JSON format.
